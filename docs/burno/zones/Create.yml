info:
  name: Create
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url_zones}}"
  body:
    type: json
    data: |-
      {
        "name": "google.com",
        "forward_policy": "custom",
        "forward_zone_id": "019e8780-04c4-7710-9be4-72dc539974e3",
        "ttl": 300
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
