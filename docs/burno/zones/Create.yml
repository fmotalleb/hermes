info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url_zones}}"
  body:
    type: json
    data: |-
      {
        "name": "local",
        "ttl": 300
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
