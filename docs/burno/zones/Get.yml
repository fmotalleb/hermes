info:
  name: Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url_zones}}?limit=50"
  params:
    - name: limit
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
