info:
  name: Get By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url_zones}}/{{zone_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

