info:
  name: Update
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url_zones}}/{{zone_id}}"
  body:
    type: json
    data: |-
      {
        "name": "google.com",
        "forward_zone_id" : "019e46fa-b559-7e6e-b9ed-a850acfe327e",
        "ttl": 300
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
