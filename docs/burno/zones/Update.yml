info:
  name: Update
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url_zones}}/{{zone_id}}"
  body:
    type: json
    data: |-
      {
        "name": "dornica.net",
        "forward_policy": "custom",
        "forward_zone_id": "019e8301-faee-736e-a94d-37b357470059",
        "ttl": 300
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
