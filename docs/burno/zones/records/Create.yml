info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url_zone_records}}"
  body:
    mimeType: application/json
    text: |
      {
        "name": "www",
        "type": "A",
        "value": "192.0.2.10",
        "ttl": 300,
        "priority": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

