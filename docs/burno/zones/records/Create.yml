info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url_zone_records}}"
  body:
    type: json
    data: |-
      {
        "name": "test",
        "type":"TXT",
        "value":"basetxt"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
