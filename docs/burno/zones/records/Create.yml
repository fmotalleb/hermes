info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url_zone_records}}"
  body:
    type: json
    data: |-
      {
        "name": "test",
        "type":"A",
        "value":"127.0.0.1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
