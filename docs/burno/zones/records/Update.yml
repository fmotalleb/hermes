info:
  name: Update
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url_zone_records}}/{{record_id}}"
  body:
    mimeType: application/json
    text: |
      {
        "name": "www",
        "type": "A",
        "value": "192.0.2.11",
        "ttl": 300,
        "priority": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

