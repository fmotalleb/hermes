info:
  name: Update
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url_zone_records}}/{{record_id}}"
  body:
    type: json
    data: |-
      {
        "name": "test",
        "type":"A",
        "value":"1.1.1.1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
