info:
  name: Update
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url_zone_records}}/{{record_id}}"
  body:
    type: json
    data: |-
      {
        "name": "test",
        "type":"TXT",
        "value":"updatetxt"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
