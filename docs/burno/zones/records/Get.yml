info:
  name: Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url_zone_records}}?limit=5"
  params:
    - name: limit
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

