info:
  name: Get Hijack
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url_hijacks}}/{{hijack_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
