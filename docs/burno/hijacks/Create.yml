info:
  name: Create Hijack
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url_hijacks}}"
  body:
    type: json
    data: |-
      {
        "name": "*.google.com",
        "value": "1.2.3.4",
        "type": "A",
        "policy": "forward",
        "forward_policy": "custom",
        "forward_zone_id": "019e8bd1-6e17-7904-ad2b-8297cc2a0eb5",
        "ttl": 300
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
