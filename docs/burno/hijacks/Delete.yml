info:
  name: Delete Hijack
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url_hijacks}}/{{hijack_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
