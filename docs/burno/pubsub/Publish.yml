info:
  name: Publish
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url_pubsub}}/{{topic}}"
  body:
    type: text
    text: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
