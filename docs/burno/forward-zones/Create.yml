info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url_forward_zones}}"
  body:
    type: json
    data: |-
      {
        "name":"cloudflare",
        "addresses": [
          "udp://1.1.1.1:53",
          "udp://1.0.0.1:53"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
