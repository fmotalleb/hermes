info:
  name: Delete
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url_forward_zones}}/{{forward_zone_id}}/delete"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

