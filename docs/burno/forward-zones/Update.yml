info:
  name: Update
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url_forward_zones}}/{{forward_zone_id}}"
  body:
    mimeType: application/json
    text: |
      {
        "name": "recursive",
        "addresses": ["9.9.9.9"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

